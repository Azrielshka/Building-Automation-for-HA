# Протокол приёмки — этап 10

Фиксация результатов приёмки (`tooling.md` §5, DoD `styleguide.md` §2).

> **⚠ Область охвата.** Это **базовый снимок приёмки этапа 10** (тег `0.5.0`).
> Приёмка последующих этапов — **11** (автояркость), **12** (метка
> `ba_area_light`) — фиксируется в changelog [`PLAN.md`](../../PLAN.md)
> («Changelog выполненных этапов»), а не здесь. Методика и метрики ниже (§6 SPEC,
> DoD) продолжают действовать для всех этапов.

**Окружение:** Home Assistant Core 2026.5.4, Python 3.14.4; песочница
`cooldrew` (HAOS). Прогон: 2026-07-27.

Статус пунктов: ✅ пройдено · ⏳ ждёт учётки/объекта/браузера.

---

## 10a — Автоматическая валидация (dev-VM + CI)

### Единый зелёный снимок

| Метрика | Порог (§6) | Факт | |
|---|---|---|---|
| pytest | все зелёные | **82 passed** | ✅ |
| Покрытие ветвей `domain/` | ≥ 95 % | **100 %** (370 stmt, 78 branch, 0 miss) | ✅ |
| Mutation score (`domain/`) | ≥ 70 % | **92.3 %** (600/650 killed) | ✅ |
| `ruff check` / `ruff format` | 0 | 0 / чисто | ✅ |
| `mypy --strict` | 0 | 0 (25 модулей; `domain/` полностью strict) | ✅ |
| `tsc --checkJs --strict` | 0 | 0 | ✅ |
| `eslint` / `prettier --check` | 0 | 0 / 0 | ✅ |

> Выжившие мутанты (50) — тексты сообщений/мест ошибок в `storage_schema` и
> эквивалентные формы JSON-ключа в `cascade`/`machine`, не бизнес-логика.
> Метрика покрытия — **по `domain/`**, не по всему пакету: обвязка исключена
> сознательно (SPEC §6, `tooling.md` §5).

### Сверка «BDD без теста = 0» (§6)

45 сценариев SPEC §5; в каждом подразделе тестов **≥** сценариев, покрытие
ветвей `domain/` 100 % → ни один сценарий не остаётся без теста.

| §5 | Модуль | Сценариев | Тестов |
|---|---|---|---|
| 5.1 | schedule | 6 | 6 |
| 5.2 | topology | 4 | 8 |
| 5.3 | profiles | 6 | 10 |
| 5.4 | cascade | 8 | 14 |
| 5.5 | machine | 14 | 19 |
| 5.6 | storage_schema | 6 | 12 |
| 5.7 | архитектурный сторож | 1 | 3 |

✅ Пройдено.

### Bronze-правила quality-scale (по коду)

Кастом-тир их не обязывает, но код им следует (`tooling.md` §6).

| Правило | Как выполнено | |
|---|---|---|
| `runtime-data` | `entry.runtime_data = coordinator`; платформы читают из него | ✅ |
| `has-entity-name` | `_attr_has_entity_name = True` во всех 4 платформах | ✅ |
| `entity-unique-id` | `_attr_unique_id` у всех сущностей | ✅ |
| `config-entry-unloading` | `async_unload_entry` → `async_unload_platforms` + снятие сервисов/панели; подписки через `entry.async_on_unload`, таймер — `timer.cancel` | ✅ |
| `entity-event-setup` | все сущности — `CoordinatorEntity` (подписка в жизненном цикле) | ✅ |

### hassfest (CI) ✅

- `.github/workflows/validate.yml` — job `hassfest` на push/PR.
  ⚠ Локально не запустить (нужен установленный `homeassistant`).
- **Результат: зелёный** (GitHub Actions, 2026-07-27) — валидация манифеста и
  интеграции без ошибок.
- `manifest.json` — ключи упорядочены (`domain`, `name`, далее по алфавиту).
- HACS-валидация **не включена**: требует метаданных репо (описание/топики),
  дала бы красный CI без пользы для приватного объекта.
- `strings.json`/переводы — **отложено**: hassfest их не требует; сущности
  работают на `_attr_name`, config_flow показывает сырые значения режимов.
  Добавить, если понадобится локализованный UI.

---

## 10b — Полевой чек-лист на песочнице (без железа)

| Проверка | Результат | |
|---|---|---|
| **fail-open гейта** | гейт `off` → шаблон `!= 'off'` = False; **исчезнувший** гейт (`unknown`, как при выгрузке) → `!= 'off'` = **True** → датчики работают | ✅ |
| 3× `reload` config entry | сущностей стабильно **10**, интеграция отвечает, без утечек/ошибок | ✅ |
| Persist конфигурации | `.storage/building_automation` v1, `opted_out_areas` пережил перезагрузки | ✅ |
| Лог | без `Detected blocking call` и deprecation по интеграции | ✅ |
| Панель: открытие/закрытие ≥ 3× | вёрстка не ломается, подписки не растут | ✅ |

---

## 10c — Требует учётки / объекта

| Пункт | Что проверить | |
|---|---|---|
| **Неадмин: панель и права** | под неадминской учёткой панель грузится, показывает мониторинг, переключатели Авто/Ручной работают; Матрица — read-only. Правка профилей на WS гейтится `@require_admin` (стандартный декоратор HA, проверено по коду; программный вызов под неадмином не гоняли по решению). | ✅ |
| — *найден и исправлен баг* | под неадмином панель висла на «Загрузка…»: `connectedCallback` ждал `subscribe_events` (неадмину запрещён) до загрузки данных. Фикс: грузим данные первыми, подписка на шину — best-effort (try/catch), живость через watched-сущности. | ✅ |
| Согласование с генератором | `ha-lighting-compilers` реализовал свою часть контракта (метки, группы этажей, гейт в blueprint) | ✅ |
| **Видимый свет** | живой DALI: смена режима → физическое вкл/выкл группового света; переопределение помещения → разворот схлопывания | ⏳ поле (объект) |
| Миграция схемы | на реальном файле старой версии (сейчас только v1, `migrate` — тождество) — при первом изменении схемы | ⏳ будущее |

---

## Definition of Done (`styleguide.md` §2)

- Автоматические критерии §6 — выполнены и измерены (10a) ✅.
- Покрытие считается по `domain/` (≥ 95 %) — 100 % ✅.
- `hassfest` — CI зелёный ✅.
- Ручной чек-лист `tooling.md` §5 — 10b пройден ✅; 10c ждёт учётки/объекта.
- Changelog SPEC/PLAN — актуализировать при закрытии этапа.

**Итог: этап 10 формально закрыт** (тег `0.5.0`, 2026-07-27). Пройдено всё
автоматически/удалённо проверяемое: 10a, 10b, неадмин (10c), hassfest зелёный,
генератор реализовал свою часть контракта. Единственный остаточный **полевой**
пункт — подтверждение **видимого света** на объекте с живым DALI (на песочнице
DALI `unavailable`, каскад доказан перехватом `call_service`); `v1.0.0` — после
этого подтверждения.
