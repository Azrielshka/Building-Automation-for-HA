# Контракт: доработки `ha-lighting-compilers` для Оркестратора здания

> Документ для агента/разработчика, работающего с репозиторием
> `ha-lighting-compilers`. Самодостаточен: контекста обсуждения читать не нужно.
>
> Составлен 2026-07-22 на основе анализа репозитория (ветка `main`, HEAD
> `d329b3d`, тег `v3.0.0`). **Пути к файлам и имена функций проверьте перед
> правкой** — они приведены как ориентир, репозиторий мог измениться.
>
> **Обновление 2026-08:** добавлена **Правка 6** (§8) — метка `ba_area_light` на
> групповой свет. Позволяет держать в Area любые световые сущности; Оркестратор
> целит помеченную. Правки 2 и §8/§9/§11 согласованы с этим. Порядок выката —
> §8.3 (метку можно ставить сейчас, «наполнять» Area прочим светом — после этапа
> 12 Оркестратора).

## 1. Зачем это нужно

Разрабатывается новая кастомная интеграция Home Assistant — **«Building
Automation» (Оркестратор здания)**. Она занимает слой, которого сегодня нет:
режимы работы здания (Урок / Перемена / Не рабочее время / Окошко), их
расписание, системные задержки и разрешение либо запрет работы автоматизаций по
датчикам движения.

Сейчас этот слой на объекте изображён вручную: пустые `input_boolean.regim_auto_<N>`
(их создаёт ваш генератор) плюс четыре рукописные YAML-автоматизации, которые по
этим флагам включают и выключают автоматизации датчиков перечислением сущностей.
Оркестратор заменяет всю эту конструкцию.

**Разграничение ответственности:**

| Владелец | Что |
|---|---|
| `ha-lighting-compilers` | всё, что выводится из проекта (Excel): Floors, Areas, групповые светильники, их назначение в Areas, метки, скрипты, автоматизации, blueprint'ы |
| Оркестратор | всё, что вычисляется в рантайме: режимы, гейты, профили, проверки инвариантов |

**Порядок развёртывания на новом объекте:** генератор первый, Оркестратор
вторым. Все правки ниже можно выкатывать **до** появления Оркестратора — они
безопасны в его отсутствие (§3.2, fail-open).

Исключения:
- **Правка 5 (§7)** — после Оркестратора: до него нет сущности, на которую
  переводится бейдж.
- **Правка 6 (§8)** — саму метку `ba_area_light` можно ставить сейчас; а
  «наполнять» Area прочим светом безопасно только после **этапа 12** Оркестратора
  (целеуказание по метке). Подробности — §8.3.

⚠ **Деплой групп и назначений в Area — двухпроходный.** На первом проходе
пакеты со светом только положены на диск, Home Assistant не перезапущен, и
сущностей групп в реестре ещё нет. Назначения Area доезжают вторым проходом
после рестарта.

---

## 2. Сводка правок

| № | Правка | Критичность |
|---|---|---|
| 1 | Гейт-условие в шаблоны blueprint | **блокирующая** — без неё Оркестратор не может выполнять свою основную функцию |
| 2 | Назначать групповые светильники в Areas | **блокирующая** — без неё Оркестратору некуда целиться |
| 3 | Метка на агрегатную Area этажа | **блокирующая** — без неё Оркестратор путает агрегат с помещением |
| 4 | Метки типа помещения на Areas | важная — без неё настройка профилей делается вручную по каждому помещению |
| 5 | Прекратить создавать `input_boolean.regim_auto_<N>` | уборка |
| 6 | Метка `ba_area_light` на групповой свет | важная — Оркестратор целит помеченную сущность; позволяет держать в Area любой свет (§8) |

---

## 3. Правка 1. Гейт-условие в blueprint'ах

### 3.1. Что делать

В шаблоны blueprint'ов системы датчиков (`blueprints/automation/zone_manager/zm_*.yaml`,
их шесть; генерируются из `generate_automations.py`) добавить:

**Вход:**

```yaml
blueprint:
  input:
    ba_gate_entity:
      name: Сущность-разрешение (Building Automation)
      description: >-
        binary_sensor Оркестратора здания, разрешающий работу по датчикам
        на этом этаже. Автоматизация выполняется, пока сущность НЕ в состоянии
        "off". Если сущность отсутствует или недоступна — работа разрешена.
      selector:
        entity:
          filter:
            domain: binary_sensor
```

**Переменная и условие — первым, до любых действий:**

```yaml
# variables рендерятся ДО проверки условий
variables:
  ba_gate_entity: !input ba_gate_entity

conditions:
  - condition: template
    value_template: "{{ states(ba_gate_entity) != 'off' }}"
```

⚠ `!input` внутри `value_template` **не подставляется** — замена идёт по узлам
YAML, а строка шаблона остаётся нетронутой. Прокидывание через `variables`
обязательно.

### 3.2. Требования, которые нельзя нарушить

**Логика строго «НЕ равно `off`», а не «равно `on`».** Это принципиально:
состояния `unavailable` и `unknown` обязаны **проходить** условие. Если
Оркестратор выгружен, обновляется или ещё не установлен, освещение по датчикам
должно работать как сейчас. Условие вида `state: "on"` оставило бы здание без
света при любом сбое интеграции — это неприемлемо для объекта с людьми.

**Именно `condition: template`, не `condition: state`.** Это исправление
первоначальной редакции контракта, где было наоборот.

Причина: `condition: state` на **несуществующей** сущности не возвращает `False`,
а бросает `ConditionErrorMessage`; `condition: not` эту ошибку не проглатывает, и
автоматизация не выполняется. То есть конструкция давала бы **fail-closed** ровно
в тот момент, когда Оркестратор ещё не установлен или перезагружается — и весь
свет по датчикам на объекте вставал бы. Проверено по исходникам тега `2026.5.4`
(`helpers/condition.py:1434-1438`, `:1204-1227`, `:1830-1855`); в `dev` защита
fail-closed только усилена.

`states()` для отсутствующей сущности возвращает `'unknown'` и не бросает, поэтому
шаблонный вариант держит fail-open во всех трёх случаях: сущности нет,
`unavailable`, `unknown`.

**Не используйте `has_value()`** — он даёт `False` и для отсутствующей сущности, и
для `unavailable`, то есть возвращает нас к fail-closed.

Стоимость шаблона приемлема: компилируется один раз при загрузке автоматизации,
подписок не создаёт, на срабатывании — рендер тривиального выражения.

⚠ **Обратная сторона:** опечатка в имени гейта не будет замечена — условие просто
всегда истинно, и Оркестратор никогда не сможет запретить работу датчиков.
Поэтому имя подставляется генератором по формуле из §3.3 и руками не вводится.

**Гейт нужен и в ON-, и в OFF-блюпринтах.** Не только во включающих. Иначе в
режиме, где датчики запрещены и свет статически включён на 100 %, OFF-автоматизация
по таймауту вакансии всё равно погасит его.

### 3.3. Чем заполнять вход

Оркестратор публикует по одной сущности на этаж. Гарантированный формат:

```
binary_sensor.building_automation_sensors_allowed_floor_<N>
```

где `<N>` — номер этажа из колонки «Этаж» (то же значение, что идёт в `level`
при создании Floor).

Генератор подставляет это значение в экземпляры автоматизаций при генерации.
Проверять существование сущности не нужно — отсутствующая безопасна по §3.2.
Значение подставляется **только генератором**: ручной ввод создаёт риск
незамеченной опечатки (§3.2).

### 3.4. Как проверить

- Автоматизация с гейтом, указывающим на несуществующую сущность, срабатывает и
  доходит до конца — свет по датчикам включается.
- Та же автоматизация при сущности в `off` завершается на условии; в трейсе
  видно `condition not met`, вызова `zone_manager.get_sensor_config` нет.
- При сущности в `on` — работает штатно.

---

## 4. Правка 2. Назначение групповых светильников в Areas

### 4.1. Политика

**В каждую управляемую Area назначить групповой светильник** её уровня:

| Area | Что назначить |
|---|---|
| Area помещения (`103_Вестибюль`) | `light.<room_slug>_obshchii` |
| Агрегатная Area этажа (`Весь <N> этаж`) | `light.ves_<N>_i_etazh` |

> **Обновлено (Правка 6, §8).** Раньше здесь требовалась **ровно одна** `light` в
> Area, а лампы и зонные группы не допускались — иначе целеуказание по `area_id`
> било бы по всем и удваивало трафик. С Правкой 6 Оркестратор целит сущность с
> меткой **`ba_area_light`**, поэтому прочие световые сущности в Area
> **допустимы** (удобно для дашбордов, голосовых команд, DALI `suggested_area`).
> Обязательное: групповой свет назначен в Area **и** несёт `ba_area_light`, и
> такой помеченный — ровно один. Порядок выката прочего света — §8.3.

Датчики и панели (`sensor.ms_*`, `sensor.il_*`, `event.kp_*`) в Area помещения
остаются — они не домена `light` и целеуказанию по свету не мешают.

### 4.2. Зачем именно так

Оркестратор целит **конкретную** помеченную сущность (`ba_area_light`, §8), а не
`area_id`, — поэтому прочие светильники Area команд от него не получают, двойного
трафика на шине нет даже при лампах и группе рядом. (В ранней редакции, до Правки
6, целью был `area_id`, и лишний свет действительно удваивал команды — отсюда
прежнее требование «одна `light` в Area».)

Групповой свет всё равно **должен быть назначен в Area**: по нему Оркестратор
находит светильник помещения (помеченный `ba_area_light` **и** в этой Area).
Заодно корректны карточки дашборда и голосовые команды «выключи свет в 103».

### 4.3. Техническая сторона

Сейчас генератор Areas только **создаёт** (`config/area_registry/create` в
`scripts/_lib/ha_ws.py`) и ни одной сущности в них не назначает —
`config/entity_registry/update` не вызывается нигде. Это новая способность.

Назначение делается вызовом:

```
config/entity_registry/update  { entity_id: "...", area_id: "..." }
```

`area_id` возвращает Home Assistant при создании Area — генератор его уже
получает.

⚠ **Ловушка, уже описанная в вашей документации** (`data_model.md`, `canon.py`):
у YAML-групп `unique_id` **не равен** `entity_id`. Реальный `entity_id` выводится
из отображаемого имени через slugify, а `unique_id` только регистрирует запись в
реестре. Назначать area нужно по `entity_id`, построенному билдерами канона
(`general_light_entity()`, `floor_light_entity()`), а не по `unique_id`.

### 4.4. Технические помещения — решено

Группа тех.помещений этажа `light.tekh_pom_<N>_i_etazh` **ни в какую Area не
назначается**. Технические помещения выведены из управления по расписанию
(решение владельца, 2026-07-22): освещение в них остаётся зоной ответственности
человека.

Метка opt-out для них **не нужна**: у технических помещений нет собственных Area
в реестре (группа `light.tekh_pom_<N>_i_etazh` агрегирует помещения, отдельными
Area не представленные). Оркестратор их попросту не видит — нарушать инвариант
нечему. Требование пометить их opt-out, фигурировавшее в ранней переписке,
снято.

### 4.5. Как проверить

- В каждой Area помещения назначен `*_obshchii`, в агрегатной Area этажа —
  `light.ves_<N>_i_etazh` (и они несут `ba_area_light`, §8).
- Прочий свет в Area **допустим** (Правка 6), но выкатывается по §8.3.
- Повторный деплой не создаёт дублей и не переназначает уже назначенное.

---

## 5. Правка 3. Метка на агрегатную Area этажа

### 5.1. Проблема

На каждый этаж приходятся две Area, принадлежащие одному Floor:

- Area помещения — `103_Вестибюль`;
- агрегатная Area этажа — `Весь 1 этаж` (`ves_1_etazh`), заведённая ради
  карточки `type: area`, поскольку карточки для Floor в HA нет.

Оркестратор перебирает Areas этажа. Не отличив агрегатную от помещений, он
отправит команду и на этажную группу, и на каждое помещение — то самое двойное
воздействие, только с другой стороны.

Опознавать по имени (`ves_<N>_etazh`) **нельзя**: `area_id` выводится из имени
при создании, а переименование в интерфейсе HA его не меняет — связь по имени
разъедется.

### 5.2. Что делать

Создать метку и проставить её на все агрегатные Areas этажей.

- **Имя метки:** `ba_floor_area` (ASCII — чтобы `label_id` был предсказуем и
  совпал с именем; транслитерация кириллицы дала бы нестабильный идентификатор).
- API: `config/label_registry/create`, затем `config/area_registry/update` с
  добавлением `label_id` в список меток Area.
- Идемпотентность — по имени метки, как уже сделано для Areas.

Если предпочитаете другое имя — согласуйте, оно жёстко зашивается в Оркестратор.

### 5.3. Как проверить

Все агрегатные Areas этажей несут метку `ba_floor_area`; ни одна Area помещения
её не несёт. Повторный деплой дублей не создаёт.

---

## 6. Правка 4. Метки типа помещения

### 6.1. Что делать

Проставить на каждую Area помещения метку, соответствующую значению колонки
«Тип помещения». Шесть значений (из `canon.py`): `class`, `korridor`,
`recreation`, `zal`, `special`, `hall`.

Имена меток — с префиксом, ASCII, чтобы `label_id` был предсказуем:

```
ba_type_class      ba_type_korridor    ba_type_recreation
ba_type_zal        ba_type_special     ba_type_hall
```

### 6.2. Зачем

Оркестратор позволяет задать профиль режима сразу всем помещениям одного типа —
например, «во всех коридорах на Перемене свет 100 %». Без меток инженеру
пришлось бы настраивать сорок помещений поштучно, что на пусконаладке
гарантированно приводит к пропускам.

Данные для этого у вас уже есть — колонка заполнена в исходной таблице.

### 6.3. Как проверить

Каждая Area помещения несёт ровно одну метку вида `ba_type_*`, соответствующую
её типу в таблице. Агрегатные Areas этажей меток типа не несут.

---

## 7. Правка 5. Убрать `input_boolean.regim_auto_<N>`

Прекратить генерацию этих helper-сущностей (сейчас — в `generate_helpers.py`,
пакет `lighting_compilers:`).

Они создавались как «голый выключатель без логики»; логику к ним дописывали
вручную четырьмя YAML-автоматизациями, которые дёргали `automation.turn_on/off`
списками. Всю эту конструкцию заменяет гейт из §3.

⚠ Удаление из генерируемого файла **не удаляет** сущности, уже существующие на
объекте: они останутся в реестре осиротевшими. На новых объектах вопрос не
возникает; для существующих потребуется ручная уборка.

### 7.1. Второй потребитель: бейдж на этажном view

У этих helper'ов есть потребитель помимо ручной обвязки — **бейдж режима на
этажном view дашборда**. Просто прекратить генерацию значит оставить дырку на
каждом этаже.

Бейдж был **кликабельным переключателем** ручного режима этажа. Замена —
`switch.building_automation_floor_<N>_automatic` (тумблер: ON = этаж следует
расписанию, OFF = ручной стоп). Полярность совпадает с legacy `regim_auto`
(ON = автоматика), диспетчер не переучивается.

**Не гейт.** Гейт `binary_sensor.building_automation_sensors_allowed_floor_<N>`
— вычисляемый и read-only: кликать по нему нельзя. Легаси `regim_auto` совмещал
две роли (его и писал автомат по режиму, и кликал диспетчер) — это конфликт двух
писателей, из-за которого смена режима затирала ручной выбор. Оркестратор их
разделяет: `switch` — вход (человек), гейт — выход (вычисление).

⚠ Для макета: тумблер показывает «этаж заглушен вручную?», а не «датчики сейчас
работают?». В «Перемене» датчики выключены профилем режима, но тумблер при этом в
положении «автоматика». Если на бейдже нужно видеть фактическое состояние
датчиков — это отдельный индикатор на гейт рядом с тумблером.

Правка 5 выполняется **после** появления Оркестратора — до этого замены не
существует.

**`input_number.vacant_delay` не трогать** — он относится к системе датчиков и
остаётся.

---

## 8. Правка 6. Метка `ba_area_light` на групповой свет

### 8.1. Что делать

Пометить **групповой светильник** каждой управляемой Area меткой `ba_area_light`.
Оркестратор целит именно помеченную сущность, а не «единственную `light` в Area»,
— поэтому в Area теперь могут лежать и другие световые сущности (лампы, зонные
группы), не ломая каскад.

Метить две категории:

| Что метить | Сущность |
|---|---|
| групповой свет помещения | `light.<room_slug>_obshchii` |
| групповой свет этажа (агрегат) | `light.ves_<N>_i_etazh` |

Одна метка на обе категории: в Area помещения и в агрегатной Area этажа лежат
разные светильники, и в каждой ровно один помеченный.

### 8.2. Техническая сторона

- **Создать метку:** `config/label_registry/create` — идемпотентно по имени, как
  для `ba_floor_area` (§5).
- **Проставить на сущность:** `config/entity_registry/update { entity_id, labels: [...] }`.
  ⚠ **Дополнять** список меток, а не затирать: у сущности уже могут быть метки.
  Прочитать текущие, добавить `ba_area_light`, записать назад.
- `entity_id` строить **каноном** (`general_light_entity()`, `floor_light_entity()`),
  не из `unique_id` — та же ловушка `unique_id ≠ entity_id`, что в §4.3.
- Идемпотентность — по имени метки и её наличию в списке сущности; повторный
  деплой дублей не плодит.

### 8.3. Порядок и совместимость с текущим Оркестратором

⚠ **Разделить на два шага** — иначе можно молча выбить помещения из управления:

1. **Создать метку и проставить её на групповой свет — безопасно сейчас.**
   Групповой свет пока остаётся единственной `light` в Area, метка на нём текущий
   Оркестратор (`0.6.0`) не смущает.
2. **Класть в Area прочий свет (лампы, зонные группы) — только после этапа 12
   Оркестратора** (целеуказание по метке). До него действует инвариант «ровно
   одна `light` в Area» из `0.6.0`: лишний свет даст `INVARIANT_BROKEN`, и
   помещение молча выпадет из управления.

То есть саму метку выкатывайте сейчас, а «наполнять» Area всем светом —
синхронно с релизом Оркестратора этапа 12.

### 8.4. Как проверить

- Каждый групповой свет помещения (`*_obshchii`) и этажа (`ves_*`) несёт метку
  `ba_area_light`; на управляемую Area — ровно один помеченный.
- Существующие метки сущности сохранены (не затёрты).
- Повторный деплой дублей не создаёт.

---

## 9. Чего делать НЕ нужно

- **Не создавать сущности состояния Оркестратора** — режимы, гейты,
  «текущий режим здания». Это вычисляемое состояние; второй владелец означал бы
  расхождение после каждого перезапуска HA. Именно по этой причине отвергнут
  вариант с переключением автоматизаций через `automation.turn_on/off`.
- **Не проверять инвариант Area при деплое.** За ним следит Оркестратор в
  рантайме: с Правкой 6 инвариант — «ровно одна сущность с меткой `ba_area_light`
  в управляемой Area» (0 или ≥2 → каскад по Area не выполняется, помечается
  ошибкой конфигурации).
- **Не экспортировать карту топологии** для Оркестратора: цель он выводит из Area
  (помеченный `ba_area_light` светильник), сопоставление не нужно.
- **Лампы и зонные группы в Areas** — ограничение **снято Правкой 6** (§8):
  прочий свет в Area допустим. Выкат — по §8.3 (после этапа 12 Оркестратора).

---

## 10. Что гарантирует Оркестратор со своей стороны

### 10.1. Публичные сущности

Имена задаются Оркестратором по умолчанию и без согласования не меняются.
`entity_id` выводится из имени при первом создании и может быть изменён
пользователем в интерфейсе — гарантируются значения по умолчанию.

**Уровень здания:**

| Сущность | Смысл |
|---|---|
| `select.building_automation_control_mode` | режим управления: Авто / Ручной |
| `sensor.building_automation_schedule_mode` | режим расписания |
| `binary_sensor.building_automation_schedule_source` | доступен ли источник расписания |
| `sensor.building_automation_pending_transition` | отложенный переход; атрибут — момент применения |

**Уровень этажа** (`<N>` — номер этажа из колонки «Этаж»):

| Сущность | Смысл |
|---|---|
| `switch.building_automation_floor_<N>_automatic` | режим этажа тумблером: ON = автоматика (по зданию), OFF = ручной стоп |
| `binary_sensor.building_automation_sensors_allowed_floor_<N>` | **гейт** (§3) |
| `sensor.building_automation_floor_<N>_applied_mode` | фактически применённый режим этажа |

### 10.2. Прочие гарантии

- Не создаёт и не изменяет Floors, Areas, групповые светильники, скрипты,
  автоматизации и blueprint'ы — всё это остаётся вашим. Метку `ba_area_light`
  Оркестратор только **читает**, не ставит (её ставит генератор, §8).
- Не пишет в файлы с префиксами `zm_` / `zm-`.
- Не вызывает `automation.turn_on` / `automation.turn_off`.
- Читает реестр (Floors, Areas, метки) и реагирует на его изменения; удаление
  или перенос Area не приводит к падению.

---

## 11. Открытые вопросы

1. **Имена меток — согласованы**, жёстко зашиты в Оркестратор: `ba_floor_area`
  (§5), `ba_type_*` (§6), `ba_area_light` (§8, решение 2026-08). Метка opt-out
  **не нужна** — opt-out ведёт Оркестратор в `.storage` (решение Q3=C).
2. **Интерактивность бейджа** решена: `switch` этажа (§7.1). **opt-out
  технических помещений** снят: у них нет Area (§4.4).
3. **Поведение DALI-интеграции (`suggested_area`) — более не риск.** Раньше
  автоназначение ламп в Area грозило нарушением инварианта «одна `light`»; с
  Правкой 6 (§8) Оркестратор целит помеченный `ba_area_light` светильник, и
  лишние лампы в Area безвредны. Периодическая уборка `area` у ламп не требуется.

---

## 12. Источник требований

Полная архитектура Оркестратора и обоснование каждого решения —
`Main-management-system/docs/TZ.md` (редакция 5), разделы §3 (политика Area),
§6 (гейт) и §7 (разграничение). Постановка задачи — там же, `docs/task.md`.
